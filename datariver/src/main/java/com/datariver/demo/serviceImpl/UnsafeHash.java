package com.datariver.demo.serviceImpl;

public class UnsafeHash extends UnsafeTree{

    public long put(long address) {
        return put(address);
    }

    public long get(long address) {
        return get(address);
    }

    public boolean remove(long address) {
        return remove(address);
    }
}
